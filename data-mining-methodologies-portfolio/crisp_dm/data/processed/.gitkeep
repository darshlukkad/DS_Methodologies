# Processed Data Directory

This directory contains cleaned, transformed, and feature-engineered datasets ready for modeling.

## Generated Files

After running the Data Preparation phase, you'll find:

### Training/Validation/Test Splits
- `train_features.parquet` - Training features (time-aware split)
- `train_target.parquet` - Training target (Weekly_Sales)
- `val_features.parquet` - Validation features
- `val_target.parquet` - Validation target
- `test_features.parquet` - Test features
- `test_target.parquet` - Test target (if available)

### Feature Engineering Artifacts
- `feature_pipeline.joblib` - Sklearn preprocessing pipeline
- `feature_names.json` - List of all engineered features
- `scaler.joblib` - Feature scaler (if used)

### Data Quality Reports
- `data_quality_report.html` - Automated data profiling
- `leakage_check_results.json` - Data leakage validation

## Feature Categories

### Temporal Features
- `year`, `month`, `week`, `day_of_week`
- `is_month_start`, `is_month_end`, `is_quarter_end`
- `weeks_to_christmas`, `weeks_to_thanksgiving`, `weeks_to_super_bowl`

### Lag Features (by Store-Dept)
- `sales_lag_1`, `sales_lag_2`, `sales_lag_4`, `sales_lag_52`
- `sales_rolling_mean_4`, `sales_rolling_std_4`
- `sales_rolling_mean_8`, `sales_rolling_mean_52`

### Holiday & Event Features
- `is_holiday` (original)
- `is_super_bowl`, `is_labor_day`, `is_thanksgiving`, `is_christmas`
- `holiday_week_before`, `holiday_week_after`

### Markdown Features
- `markdown_total` - Sum of MarkDown1-5
- `markdown_active_count` - Number of active markdowns
- `markdown_x_size` - Markdown interaction with store size

### Store Features
- `store_type_A`, `store_type_B`, `store_type_C` (one-hot)
- `size_normalized` - Store size (normalized)
- `size_quartile` - Store size category

### Department Features
- `dept_category` - Grouped department categories
- `dept_x_is_holiday` - Department-holiday interaction

## Data Formats

- **Parquet**: Used for efficient storage and fast I/O
- **Joblib**: Used for sklearn objects (pipelines, models)
- **JSON**: Used for metadata and configuration

## Size Estimates

- Raw data: ~5 MB
- Processed features: ~15-20 MB (with lag/rolling features)

## Notes

- All files are excluded from git (large data files)
- Reproducible via notebook execution
- Version control handled by MLflow data versioning

---

*Place .gitkeep file to track this directory in git*
